import pygame
import math
from collections import deque
#Width of screen
WIDTH = 800
#set display window
WIN = pygame.display.set_mode((WIDTH, WIDTH))
pygame.display.set_caption("Npuzzle with depth-first search")
#Set colour
WHITE = (255, 255, 255)
GREY = (128, 128, 128)

def main(win, width):
    run = True
    while run:
    
    pygame.quit()

main(WIN, WIDTH)